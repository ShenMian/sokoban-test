[package]
name = "extension"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
godot = "0.5"
soukoban = { path = "../../soukoban" }
rusqlite = { version = "0.40", features = ["bundled", "serialize"] }
